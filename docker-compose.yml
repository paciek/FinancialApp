services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: financialapp_app
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    depends_on:
      - db

  web:
    image: nginx:alpine
    container_name: financialapp_web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app

  db:
    image: mysql:8.4
    container_name: financialapp_db
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_DATABASE: financialapp
      MYSQL_USER: financialapp
      MYSQL_PASSWORD: financialapp
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: financialapp_pma
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_USER: financialapp
      PMA_PASSWORD: financialapp
    depends_on:
      - db

volumes:
  dbdata:
